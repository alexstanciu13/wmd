# SMTP Configuration for Email Sending
# =====================================

# Zoho Mail SMTP (Default)
SMTP_HOST=smtp.zoho.eu
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=contact@webmediadesign.ro
SMTP_PASS=your_zoho_mail_password_or_app_password_here
SMTP_FROM=Web Media Design <contact@webmediadesign.ro>

# Alternative: Hostinger SMTP (if Zoho is blocked)
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=contact@webmediadesign.ro
# SMTP_PASS=your_hostinger_email_password_here
# SMTP_FROM=Web Media Design <contact@webmediadesign.ro>

# Server Configuration
# ====================
PORT=3001
NODE_ENV=production

# Instructions:
# 1. Copy this file to .env (or rename it)
# 2. Replace placeholder values with actual credentials
# 3. Never commit .env to Git (already in .gitignore)
# 4. Set restrictive file permissions: chmod 600 .env
# 5. See HOSTINGER_DEPLOYMENT.md for detailed setup guide
